---
title: 최신 프론트 기술로 블로그 만들기
date: 2026-06-11
description: 이 블로그를 어떤 기술로, 왜 그렇게 설계했는지 정리한 개발기
tags: [Front End, Next.js, 블로그]
---

# 들어가며

개발자라면 한 번쯤 블로그를 직접 만들어보고 싶어 한다. 나도 그랬다. 처음에는 만들어진 플랫폼을 썼는데, 디자인 커스텀이 자유롭지 않고 글쓰기 경험도 아쉬웠다. "이럴 거면 내가 만들자" 싶어서 결국 직접 만들기로 했다.

기왕 만드는 김에 **요즘 프론트엔드 기술을 제대로 한번 써보자**는 욕심도 있었다. 그렇게 만든 결과물이 지금 이 블로그다. 이 글에서는 이 블로그를 어떤 기술로, 그리고 **왜 그 기술을 골랐는지**를 정리해 본다.

# 어떤 블로그를 만들고 싶었나

기술을 고르기 전에 요구사항부터 정리했다. 세 가지였다.

1. **콘텐츠는 마크다운(MDX)으로** — 글은 텍스트 파일로 작성하고, 인터랙티브한 요소도 넣을 수 있어야 한다.
2. **빠른 로딩** — 블로그는 읽는 곳이다. 정적 사이트로 빌드해 첫 화면이 바로 떠야 한다.
3. **자유로운 커스텀** — 다크모드, 좋아요, 댓글, 코드 복사 같은 기능을 원하는 대로 붙일 수 있어야 한다.

이 세 가지를 기준으로 스택을 골랐다.

# 사용한 기술 한눈에 보기

블로그도 하나의 프로젝트다. 필요한 기술을 역할별로 나눠보면 이렇다.

- **프레임워크** : Next.js
- **콘텐츠 레이어** : Velite (+ MDX)
- **스타일링** : Tailwind CSS
- **린터·포매터** : Biome
- **좋아요 스토어** : Upstash Redis
- **댓글** : Giscus
- **호스팅** : Vercel

하나씩 살펴보자.

# Next.js

블로그는 대부분 **SSG**(정적 사이트 생성)로 만든다. 빌드 타임에 모든 페이지를 HTML로 미리 만들어두는 방식이다. 요청이 오면 완성된 HTML을 그대로 내려주니 빠르고, 서버 부담도 적다.

SSG를 지원하는 도구는 많지만 나는 **Next.js**를 골랐다. 이유는 단순하다. **가장 익숙하고, 레퍼런스가 가장 많기 때문이다.** 막히는 부분이 생겨도 검색하면 답이 나온다는 건 생각보다 큰 장점이다.

App Router에서는 동적 경로의 페이지를 미리 생성하기 위해 `generateStaticParams`를 쓴다. 글 목록을 넘기면 각 `slug`에 해당하는 페이지를 빌드 타임에 정적으로 만들어준다.

```tsx
// 각 slug에 대한 페이지를 빌드 타임에 정적 생성한다
export async function generateStaticParams() {
  return posts.map((post) => ({ slug: post.slug }));
}
```

이 블로그는 **App Router**와 **React 19 + React Compiler**를 쓴다. React Compiler는 메모이제이션을 자동화해주는 컴파일러다. 기존에는 불필요한 리렌더를 막으려고 `useMemo`, `useCallback`을 직접 붙여야 했는데, 이제는 컴파일러가 알아서 처리한다. 보일러플레이트가 줄고 코드가 깔끔해진다.

# Velite

가장 핵심적인 고민은 **MDX를 어떻게 렌더링 가능한 콘텐츠로 가공할 것인가**였다.

이 블로그는 마크다운이 아니라 **MDX**로 글을 쓴다. MDX는 마크다운 안에서 JSX(리액트 컴포넌트)를 쓸 수 있게 해주는 포맷이다. 덕분에 글 안에 버튼이나 인터랙티브한 요소를 직접 넣을 수 있다.

문제는 MDX를 번들로 컴파일하는 빌드 설정이 까다롭다는 것이다. 그래서 이 과정을 추상화해주는 **콘텐츠 레이어(content layer)** 도구가 필요했고, **Velite**를 골랐다.

## Velite가 하는 일

Velite는 `content/posts/` 아래의 MDX를 **빌드 타임에 타입이 보장된 데이터로 변환**해준다. 변환 결과는 코드에서 `import`해 바로 쓸 수 있다.

가장 큰 장점은 **스키마로 콘텐츠의 타입을 정의**할 수 있다는 점이다. Velite의 스키마(`s`)는 내부적으로 **Zod** 위에서 동작해, Zod와 같은 문법으로 각 필드의 타입과 필수 여부를 선언한다.

```ts
schema: s.object({
  title: s.string().max(99),          // 필수, 99자 이내
  date: s.isodate(),                  // 필수, ISO 날짜
  description: s.string().optional(), // 선택
  tags: s.array(s.string()).default([]),
  code: s.mdx(),       // MDX → 컴파일된 함수 문자열
  toc: s.toc(),        // 목차 자동 추출
  metadata: s.metadata(), // 읽는 시간 등 자동 계산
})
```

스키마를 벗어난 글(예: `date` 누락)이 있으면 **빌드 단계에서 에러로 잡힌다.** 메타데이터를 빠뜨린 채 배포하는 사고를 막아준다.

또 `toc`, `metadata` 같은 **computed 필드**를 지원한다. 본문을 파싱해 목차와 예상 읽기 시간을 자동으로 만들어준다. 지금 이 글 오른쪽 목차도 이렇게 생성된 것이다.

> 같은 역할을 하던 **Contentlayer**가 한동안 표준처럼 쓰였지만 지금은 유지보수가 멈췄다. Velite는 그 대안으로 나온 더 가볍고 최신인 도구다.

## 코드 하이라이팅

개발 블로그라 코드 블록 가독성이 중요하다. 신택스 하이라이팅은 `rehype-pretty-code`와 **Shiki**가 담당한다. Shiki는 VS Code와 같은 TextMate 문법으로 토큰을 칠한다. 핵심은 이 작업을 **빌드 타임에 수행**한다는 점이다. 클라이언트는 추가 JS 다운로드 없이 이미 색이 입혀진 마크업을 받는다.

코드 블록의 **복사 버튼**은 직접 구현했다. 렌더 후 `pre` 요소를 모두 찾아 버튼을 주입하고, 클립보드 API로 복사한다.

# Tailwind CSS

스타일링은 **Tailwind CSS**로 했다. `text-xl`, `mb-6` 같은 **유틸리티 클래스**를 마크업에 직접 조합하는 방식이라, 별도 CSS 파일이나 클래스 네이밍 고민 없이 빠르게 스타일링할 수 있다.

이 블로그는 **Tailwind v4**를 쓴다. v4는 설정을 JS 파일이 아니라 CSS 안에서 한다. `@theme` 블록에 색상·폰트 같은 디자인 토큰을 선언한다.

## 다크모드

다크모드는 **CSS 변수(커스텀 프로퍼티)** 기반으로 구현했다. 색을 직접 박지 않고 변수로 참조한 뒤, 테마에 따라 변수 값만 교체하는 방식이다.

```css
[data-theme="light"] {
  --bg-primary: #fdfcfb;
  --text-primary: #1c1c1c;
}

[data-theme="dark"] {
  --bg-primary: #121211;
  --text-primary: #edece9;
}
```

이러면 테마를 바꿀 때 변수 값만 갈아끼우면 전체가 한 번에 전환된다. 유틸리티마다 `dark:` 프리픽스를 붙일 필요가 없다.

여기서 흔히 만나는 문제가 **FOUC**(다크모드인데 첫 프레임이 흰색으로 번쩍이는 현상)다. 하이드레이션 전에 화면이 먼저 그려져서 생긴다. 그래서 `<head>` 최상단에 **인라인 스크립트**를 둬서, 렌더 전에 `localStorage`의 테마를 먼저 적용한다.

```tsx
// 렌더 전에 저장된 테마를 적용해 FOUC 방지
const themeScript = `
  const theme = localStorage.getItem("theme");
  if (theme === "light" || theme === "dark") {
    document.documentElement.setAttribute("data-theme", theme);
  }
`;
```

# Biome

코드 품질 도구로는 보통 **린터**(ESLint)와 **포매터**(Prettier)를 같이 쓴다. 설정이 번거롭고 속도도 느린 편이다.

이 블로그는 둘을 통합한 **Biome**를 쓴다. 린트와 포맷을 하나의 도구로 처리하고, Rust로 작성돼 **매우 빠르다.** 설정 파일도 하나(`biome.json`)면 끝이다.

```bash
pnpm lint    # biome check
pnpm format  # biome format --write
```

# 좋아요 — 서버 연동

처음에는 좋아요를 `localStorage`에만 저장했다. 하지만 그러면 다른 사용자의 좋아요가 공유되지 않고, 기기를 바꾸면 사라진다. 그래서 **서버에 영속화**하도록 다시 설계했다.

스토어로는 **Upstash Redis**를 골랐다. 서버리스 환경에 맞는 Redis로, 유휴 시 비용이 0에 수렴해 개인 블로그에 적합하다.

좋아요 처리는 **서버 액션**(Server Action)으로 했다. 버튼 클릭이 곧바로 서버 함수 호출로 이어져, 별도 API 라우트를 만들 필요가 없다.

설계 포인트는 다음과 같다.

- **멱등성** : 카운터를 `+1`하는 대신 **Set 자료구조에 사용자 ID를 SADD/SREM**한다. SADD/SREM은 멱등이라 같은 요청이 중복돼도 안전하고, 좋아요 수는 `SCARD`(집합 크기)로 센다. 음수나 중복이 구조적으로 불가능하다.
- **익명 식별자** : 로그인이 없으므로 방문자에게 `httpOnly` 쿠키로 익명 UID를 발급한다. `localStorage`만 지워서는 우회되지 않는다.
- **레이트 리밋** : `@upstash/ratelimit`의 슬라이딩 윈도우로 1분에 20회로 제한해 연타·어뷰징을 막는다.
- **낙관적 업데이트** : 클릭 즉시 UI에 반영하고, 서버 응답이 실패하면 직전 확정 상태로 롤백한다.

```ts
// 좋아요 = SADD, 취소 = SREM. 개수는 SCARD로 센다.
if (liked) {
  await redis.sadd(key, uid);
} else {
  await redis.srem(key, uid);
}
const likes = await redis.scard(key);
```

# 댓글 — Giscus

댓글을 직접 구현하면 DB와 스팸 관리가 부담이다. 그래서 **Giscus**를 붙였다. **GitHub Discussions를 댓글 백엔드로 사용하는** 위젯이라, 댓글이 내 저장소의 Discussion에 쌓이고 별도 서버가 필요 없다. 인증도 GitHub 계정으로 처리된다.

블로그 테마와 댓글창 테마를 맞추기 위해, `MutationObserver`로 `data-theme` 변경을 감지해 `postMessage`로 Giscus iframe 테마를 동기화했다.

# 그 밖의 장치들

읽기 경험을 위한 작은 기능들도 넣었다.

- **읽기 진행률 바** : 스크롤에 따라 차오르는 상단 인디케이터.
- **목차(TOC)** : Velite가 추출한 헤딩으로 만든 사이드 목차. `sticky`로 고정된다.
- **검색** : 제목·태그 기준 검색 다이얼로그.
- **시리즈** : 연재 글을 묶어서 보여주는 시리즈 네비게이션.

# 호스팅 — Vercel과 개발 환경

배포는 **Vercel**을 썼다. Next.js 개발사라 통합이 매끄럽다. GitHub에 푸시하면 자동으로 빌드·배포되고, 커밋마다 프리뷰 배포가 생긴다. CI/CD를 따로 구성하지 않아도 된다.

로컬 개발에선 콘텐츠를 변환하는 **Velite**와 **Next.js 개발 서버**를 동시에 띄워야 한다. 매번 터미널을 두 개 켜기 번거로워서 `pnpm dev` 한 번으로 둘을 함께 실행하는 스크립트를 만들었다. 새 글의 프론트매터 템플릿을 생성하는 `pnpm new:post "제목"` 스크립트도 추가했다. 이런 자잘한 자동화가 모이면 글쓰기에만 집중할 수 있다.

# 마치며

블로그 하나를 만들면서 요즘 프론트엔드 스택을 폭넓게 다뤄봤다. 정리하면 이렇다.

- **Next.js**(App Router)로 SSG 블로그의 뼈대를 세우고
- **Velite + MDX**로 콘텐츠를 타입 안전한 데이터로 가공하고
- **Tailwind v4**로 CSS 변수 기반 다크모드까지 구현하고
- **Biome**로 린트·포맷을 통합하고
- **Upstash Redis 좋아요**와 **Giscus 댓글**로 상호작용을 더하고
- **Vercel**로 배포·프리뷰를 자동화했다.

처음엔 막연했던 개념들이 직접 부딪혀보니 하나씩 손에 익었다. 아직 채울 게 많다. 앞으로 다듬고 싶은 것들을 적으며 글을 마친다.

- 본문 전문(full-text) 검색
- 조회수 / 인기 글
- RSS 피드와 SEO 보강
- 글 안에서 코드를 실행하는 인터랙티브 예제
